# Benchmark Scenarios

This directory contains a curated subset of scenarios used for reproducible
benchmarking.  The full scenario dataset lives at `dataset/scenarios/scenarios.jsonl`.

Each JSONL file uses the schema defined in `dataset/scenarios/scenario_spec.md`.
